# Garmin Chat Desktop v4.1.0 Dependencies

# Core Application
garth==0.4.46              # Garmin Connect API wrapper
tk==0.1.0                  # Tkinter (usually included with Python)

# AI Providers
openai==1.58.1             # OpenAI, xAI, Azure OpenAI, and Ollama (OpenAI-compatible)
anthropic==0.39.0          # Anthropic Claude API
google-genai==0.3.0        # Google Gemini (new SDK)
requests==2.32.4           # HTTP library for Ollama API calls

# Document Generation
python-docx==1.1.2         # Word document creation
reportlab==4.2.5           # PDF generation
Pillow==12.1.1             # Image processing

# UI and Utilities
ttkthemes==3.2.2           # Additional ttk themes (optional)

# Build Tools (Development only)
pyinstaller==6.11.1        # Create executable
Pillow==12.1.1             # Required for PyInstaller to handle images

# Optional: For future voice features (commented out for now)
# azure-cognitiveservices-speech==1.40.0  # Voice input (future)

# Note: Some packages like tkinter come with Python
# Note: garth may need to be installed from git if not on PyPI
